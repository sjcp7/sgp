<%= form_with(model: teacher) do |form| %>
  <% if teacher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(teacher.errors.count, "error") %> prohibited this teacher from being saved:</h2>

      <ul>
        <% teacher.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :first_name, 'Primeiro nome' %>
    <%= form.text_field :first_name %>
  </div>

  <div class="field">
    <%= form.label :last_name, 'Último nome' %>
    <%= form.text_field :last_name %>
  </div>

  <%= form.fields_for :user do |u| %>
    <div class="field">
      <%= u.label :email %>
      <%= u.email_field :email, placeholder: 'exemplo@email.com' %>
    </div>

    <div class="field">
      <%= u.label :password %>
      <%= u.password_field :password %>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit 'Ok' %>
  </div>
<% end %>
