<section class="section">
  <div class="container">
    <h2 class="title"><%= @batch.description %></h2>
    <%= render 'batches/form', batch: @batch, batch_director: @batch_director, teachers: @teachers %>
  </div>
</section>

<%= javascript_pack_tag 'handle_batch_students_tabs' %>